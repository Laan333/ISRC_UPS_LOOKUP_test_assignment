services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      USER_AGENT: "ISRC_UPS_Lookup/1.0 (docker)"
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health').read()",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
